[service]
name = "turkic-api"

[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn api.main:create_app --factory --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
restartPolicyType = "on_failure"

